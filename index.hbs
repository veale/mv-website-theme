{{!< default}}
<div class="gh-page">
    <div class="gh-container">

        {{^is "paged"}}
        {{#get "pages" filter="slug:home-intro" limit="1"}}
            {{#if pages.length}}
                {{#foreach pages}}
                <section class="gh-home-intro">{{content}}</section>
                {{/foreach}}
            {{else}}
                <section class="gh-home-intro gh-home-intro--placeholder">
                    <p><strong>Add your intro text here.</strong></p>
                    <p>In Ghost Admin, create a <em>Page</em>, open settings (gear icon), set the URL slug to <code>home-intro</code>, and Publish it. Its body appears here.</p>
                </section>
            {{/if}}
        {{/get}}
        {{/is}}

        <div class="gh-postfeed">
        {{#foreach posts}}

            {{> "card" page=../pagination.page }} {{!-- partials/card.hbs --}}

        {{/foreach}}
        </div>

        {{!-- If JS isn't enabled, fallback to standard pagination --}}
        <noscript>
            <div class="gh-pagination">
                {{pagination}}
            </div>
        </noscript>
    </div>
</div>
